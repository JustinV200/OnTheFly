/* Placeholder marketplace feed route for later browsing work.
   The public listings feed is added in a later marketplace phase. */
/** Render the marketplace placeholder page. */
export function MarketplacePage(): JSX.Element {
  return <section><h2>Marketplace</h2><p>Marketplace feed will appear here.</p></section>;
}
