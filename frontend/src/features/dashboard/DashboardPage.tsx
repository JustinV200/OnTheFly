/* Placeholder dashboard route for the private expenses view.
   Real expense data arrives in a later phase without changing the route shape. */
/** Render the dashboard placeholder page. */
export function DashboardPage(): JSX.Element {
  return <section><h2>Dashboard</h2><p>Expense dashboard will appear here.</p></section>;
}
