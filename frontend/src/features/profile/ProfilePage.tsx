/* Placeholder public profile route for later phases.
   Public listing rendering is added once listings can be published. */
/** Render the profile placeholder page. */
export function ProfilePage(): JSX.Element {
  return <section><h2>Profile</h2><p>Public profile will appear here.</p></section>;
}
