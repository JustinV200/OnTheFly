/* Public surface of the demo feature: the presenter's guide page and the demo steps rail the app shell renders once.
   Progress polling, stage controls and the money views are internal. */
export { DemoGuidePage } from './DemoGuidePage';
export { DemoRail } from './rail/DemoRail';
