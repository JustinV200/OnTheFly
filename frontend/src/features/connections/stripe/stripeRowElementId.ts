/* The DOM id of the Stripe sandbox row's connect button, so another panel on the same page can send an owner straight
   to it. A shared constant rather than a literal in two files: renaming it must not silently break that jump. */
export const STRIPE_CONNECT_BUTTON_ID = 'connect-stripe-sandbox';
