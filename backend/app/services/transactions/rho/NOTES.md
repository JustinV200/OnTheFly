# Rho Integration Notes

- Verify the transaction-list endpoint, pagination shape, and sandbox data availability.
- Confirm which fields map cleanly into the normalized transaction schema.
- Do not ship live calls until credentials, rate limits, and recurring-service coverage are verified.
