"""Exposes explicit not-implemented invitation operations for the secondary path.
Secondary path — seeding marketplace supply. Not part of the demo critical path.
"""



def list_invitations() -> None:
    """Raise until outbound invitation review and approval flows are implemented."""

    raise NotImplementedError(
        "Secondary path — seeding marketplace supply. Not part of the demo critical path."
    )



def create_invitation() -> None:
    """Raise until outbound invitation sending is intentionally implemented."""

    raise NotImplementedError(
        "Secondary path — seeding marketplace supply. Not part of the demo critical path."
    )
